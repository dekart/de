#!/usr/bin/env ruby

system %{mkdir -p tmp}

if ARGV.first == 'clean'
  puts "Cleaning caches..."

  system %{rm -rf public/assets/* tmp/cache/*}
end

puts "Starting server..."

user = `whoami`.strip

system %{sudo passenger start -p 80 --user=#{ user } --max-pool-size=2}

